module.exports = {
  // Для этого MVP обычно достаточно require(...) ассетов.
  // Но этот файл оставляем, чтобы было проще расширять проект (например, подключать шрифты).
  assets: ['./assets'],
};

